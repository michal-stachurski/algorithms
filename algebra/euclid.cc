
/** @returns greatest common divisor (GCD) @b recursive */
int gcd(int a, int b) {
    return (b) ? gcd(b, a % b) : a;
}

/** @returns greatest common divisor (GCD) @b iterative */
int gcd(int a, int b) {
        
}
